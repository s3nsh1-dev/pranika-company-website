import { styled } from "@mui/material/styles";
import { Button } from "@mui/material";

export const BlueBorderButton = styled(Button)(({ theme }) => ({
  textTransform: "none",
  color: "#070066",
  border: "2px solid #070066",
}));
export const BlueButton = styled(Button)(({ theme }) => ({
  textTransform: "none",
  color: "#070066",
}));
export const NormButton = styled(Button)(({ theme }) => ({
  textTransform: "none",
}));
